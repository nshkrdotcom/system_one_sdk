# Model artifacts

Upcoming: official pinned checkpoint manifests with immutable revisions, SHA-256 checksums, cache provenance, tensor inventories, exact Nx/Axon mappings and tokenizer/preprocessor reproduction. No weights are bundled or downloaded.

Audit crucible_safetensors and hf_hub_ex before selecting dependencies. Python is an oracle for parity fixtures, not the inference runtime.
