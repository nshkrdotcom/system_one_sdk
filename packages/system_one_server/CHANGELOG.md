# Changelog

## 0.1.0 — Unreleased

- Establish independent package metadata, documentation, identity and compile smoke coverage.
- Depend on the small `system_one_contracts` seam rather than the rich client SDK.
- Runtime implementation remains under active development.
