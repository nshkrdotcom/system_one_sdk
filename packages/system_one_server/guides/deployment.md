# Deployment

No server is runnable yet. Future deployments serve remote, Python/non-Elixir, container and external System One-compatible clients.

Plan health/readiness, auth, limits, telemetry and request IDs after v1 contracts. External self_hosted_inference_core manages launch/monitor/stop and endpoint descriptors; model execution stays in the inference provider.
